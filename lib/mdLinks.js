/* Este archivo devuelve la funcionalidad de mdLinks como modulo de npm: se encarga de obtener
toda la información y de realizar todos los procesos*/
'use strict';

const mdLinks = (path, options) => {
  console.log(path + options);
};

module.exports = mdLinks;
